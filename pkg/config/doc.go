// Package config provides common config and CLI support for the sensorpush
// proxy and tools, so that each tool does not need to duplicate the common
// settings and config parsing logic.
package config
